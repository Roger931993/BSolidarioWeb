import { HasFieldInvoiceDirective } from './has-field-invoice.directive';

describe('HasFieldInvoiceDirective', () => {
  it('should create an instance', () => {
    const directive = new HasFieldInvoiceDirective();
    expect(directive).toBeTruthy();
  });
});
